# CODEX 作業ログ

## 2026-07-22

- 利用者の誤選択・候補不足への対応として、確認画面の利用者選択欄にAI候補と全利用者マスタを表示するようにした。
- GAS側で、発話された氏名・よみ・別名から候補を補完し、同じ氏名・呼び名に複数名が該当する場合は利用者を未確定にして確認を要求するようにした。
- AIプロンプトに、苗字のみなど一意に確定できない場合は候補を全件提示し、利用者IDを確定しないルールを追加した。

## 2026-08-11

- 音声解析結果の確認画面に「この結果を破棄して録音し直す」ボタンを追加した。
- 音声処理でエラーになった場合も、アプリを閉じずにエラー画面から直接録音し直せるようにした。
- 確認画面からの録音し直しでは誤操作を防ぐ確認メッセージを表示し、再録音を開始できた時点で以前の解析結果を破棄するようにした。

## 2026-08-12

- 確認画面の「AIの候補」と「全利用者」を、利用者マスタのよみを基準としたあいうえお順で表示するようにした。
- よみが未登録の利用者は氏名を基準に並べ、同じよみの場合も氏名と利用者IDで表示順が安定するようにした。
